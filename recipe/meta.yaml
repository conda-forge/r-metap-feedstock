{% set version = '1.7' %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-metap
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/metap_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/metap/metap_{{ version }}.tar.gz
  sha256: d9b511607d0e37de4428549061c5577a4e812b0f55bb7ed887d1b24711f58c42

build:
  merge_build_host: True  # [win]
  number: 3
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]
    - cross-r-base {{ r_base }}    # [build_platform != target_platform]
  host:
    - r-base
    - r-rdpack >=0.7
    - r-tfisher
    - r-lattice
    - r-mathjaxr >=0.8_3
    - r-mutoss
  run:
    - r-base
    - r-rdpack >=0.7
    - r-tfisher
    - r-lattice
    - r-mathjaxr >=0.8_3
    - r-mutoss

test:
  commands:
    - $R -e "library('metap')"           # [not win]
    - "\"%R%\" -e \"library('metap')\""  # [win]

about:
  home: http://www.dewey.myzen.co.uk/meta/meta.html
  license: GPL-2.0-only
  summary: The canonical way to perform meta-analysis involves using effect sizes. When they
    are not available this package provides a number of methods for meta-analysis of
    significance values including the methods of Edgington, Fisher, Lancaster, Stouffer,
    Tippett, and Wilkinson; a number of data-sets to replicate published results; and
    routines for graphical display.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - conda-forge/r

# Package: metap
# Type: Package
# Title: Meta-Analysis of Significance Values
# Version: 1.7
# Date: 2021-12-16
# Authors@R: person("Michael", "Dewey", role = c("aut", "cre"), email = "lists@dewey.myzen.co.uk", comment = c(ORCID = "0000-0002-7522-3677") )
# Description: The canonical way to perform meta-analysis involves using effect sizes. When they are not available this package provides a number of methods for meta-analysis of significance values including the methods of Edgington, Fisher, Lancaster, Stouffer, Tippett, and Wilkinson; a number of data-sets to replicate published results; and routines for graphical display.
# URL: http://www.dewey.myzen.co.uk/meta/meta.html
# Depends: R (>= 3.5.0)
# Imports: lattice,Rdpack (>= 0.7),TFisher,mutoss,mathjaxr (>= 0.8-3)
# RdMacros: Rdpack,mathjaxr
# License: GPL-2
# LazyLoad: yes
# NeedsCompilation: no
# Packaged: 2021-12-16 14:46:57 UTC; MichaelUser
# Author: Michael Dewey [aut, cre] (<https://orcid.org/0000-0002-7522-3677>)
# Maintainer: Michael Dewey <lists@dewey.myzen.co.uk>
# Repository: CRAN
# Date/Publication: 2021-12-16 16:50:02 UTC
